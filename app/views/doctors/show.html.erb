<h1><%= @doctor.name %></h1>
<h2><%= @doctor.department %></h2>
<h3>Appointments:</h3>
<ul>
  <% @doctor.appointments.each do |apt| %>
   <li><%= apt.appointment_datetime.strftime("%B %d, %Y at: %H:%M") %>,
     with: <%= link_to apt.patient_name, patient_path(apt.patient)%></li>
  <% end %>
</ul>
