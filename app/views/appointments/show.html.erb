<h1>Appointment</h1>

<p><%= link_to @appointment.doctor_name, doctor_path(@appointment.doctor) %></p>
<p><%= link_to @appointment.patient_name, patient_path(@appointment.patient)%></p>
<p><%= @appointment.appointment_datetime.strftime("%B %d, %Y at: %H:%M") %></p>
