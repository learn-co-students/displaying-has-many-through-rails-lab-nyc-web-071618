<h1><%= @patient.name %></h1>
<h3>Appointments:</h3>
<ul>
  <% @patient.appointments.each do |apt| %>
   <li><%= apt.appointment_datetime.strftime("%B %d, %Y at: %H:%M") %>,
     with: <%= link_to apt.doctor_name, doctor_path(apt.doctor)%></li>
  <% end %>
</ul>
